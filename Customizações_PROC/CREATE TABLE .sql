CREATE TABLE AD_TGFEMAILS (
    ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
    EMAILS VARCHAR2(1000) NOT NULL,
    CODPARC NUMBER NOT NULL,
    PRIMARY KEY (ID)
);

CREATE TABLE AD_TGFEMAILS (
    ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
    EMAIL VARCHAR2(100) NOT NULL,
    CODPARC NUMBER NOT NULL,
    PRIMARY KEY (ID)
);

CREATE TABLE AD_TGFTABBASE (
    SEQUENCIA INT NOT NULL,
    CODPROD NUMBER NOT NULL,
    GRUPOICMS NUMBER,
    GRUPOICMS2 NUMBER,
    NCM VARCHAR2(10),
    TOP NUMBER,
    IDALIQICMS NUMBER,
    VLRUNIT NUMBER,
    VLRVENDA NUMBER,
    BASEIPI NUMBER,
    PERCIPI NUMBER,
    VLRIPI NUMBER,
    BASESUBST NUMBER,
    VLRSUBST NUMBER,
    VLRBASE NUMBER,
    MVA NUMBER,
    PRIMARY KEY (SEQUENCIA,CODPROD));
